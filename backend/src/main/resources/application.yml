server:
  port: 8080

spring:
  application:
    name: qsd-admin-backend
  datasource:
    url: jdbc:mysql://localhost:3306/qsd_admin?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

app:
  jwt:
    issuer: qsd-admin
    secret: cXNkLWFkbWluLXNlY3JldC1mb3Itand0LXRva2VuLTIwMjY=
    expire-minutes: 180
